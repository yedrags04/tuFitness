# =========================================================
# CONFIGURACIÓN PARA MySQL LOCAL (XAMPP)
# =========================================================

# 1. Host: Generalmente 'localhost' o 127.0.0.1
DB_HOST=localhost 

# 2. Usuario: El usuario por defecto de MySQL en XAMPP
DB_USER=root 

# 3. Contraseña: La contraseña por defecto de MySQL en XAMPP (déjala vacía si no la has cambiado)
DB_PASSWORD=

# 4. Nombre de la Base de Datos (¡Crea esta BD primero en phpMyAdmin!)
DB_NAME=tufitness # <-- ¡IMPORTANTE! Reemplaza esto con el nombre que elijas.

# =========================================================
# OTRAS CONFIGURACIONES (Mantenidas)
# =========================================================
JWT_SECRET=unaClaveSecretaMuyDificil
PORT=5000

# Turso/libSQL (Asegúrate de que estas líneas estén comentadas o eliminadas)
# TURSO_CONNECTION_URL=...